<h2><%= @anime.title %></h2>
<h3><%= @anime.creator %></h3>
<p><%= @anime.description %></p>

<%= link_to "Edit", edit_anime_path(@anime) %>
<%= link_to "Back", root_path %>
<%= link_to "Delete", anime_path(@anime), method: :delete, data: {confirm: "Are you sure?" }%>
