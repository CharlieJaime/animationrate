<h1>Edit Anime</h1>

<%= simple_form_for @anime, html: {multipart: true} do |f|%>
  <%= f.select :category_id, @categories %>
  <%= f.input :title, label: "Animation title" %>
  <%= f.input :description %>
  <%= f.input :creator %>
  <%= f.button :submit %>
<% end %>
